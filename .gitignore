# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Database — distributed via installer, not stored in repo
*.db
*.db-shm
*.db-wal

# Environment — contains real credentials, never commit
.env

# Build outputs
dist/
installer/
lateralx.log

# Dependencies
node_modules/

# Test cache
.pytest_cache/

# Presentation files — too large for source control
*.pdf
*.pptx
LateralX_Poster.png

# Real OTRF dataset files — downloaded by fetch_real_datasets.py, not stored in repo
sample_data/**/*_real.jsonl

# Development-only scripts — not needed for end users
benchmark_real.py
diag_fp.py
fetch_real_datasets.py

# ML model files — distributed separately, not in source control
*.pkl
